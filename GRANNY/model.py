class YoloModel(object):
    def __init__(self, model_dir: str):
        self.model_dir: str = model_dir
        self.model = None

    def loadModel(self) -> None:
        # loads and initiates the model
        return None

    def segmentInstances(self) -> None:
        return

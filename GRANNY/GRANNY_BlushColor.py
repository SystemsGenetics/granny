from GRANNY import GRANNY_Base as granny


class GrannyPearBlush(granny.GrannyBase):
    def __init__(self, action, fname, num_instances, verbose):
        super(GrannyPearBlush).__init__(
            self, action, fname, num_instances, verbose
        )

    def main() -> None:
        pass
